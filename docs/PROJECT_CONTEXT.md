# הבית הצהוב — מקור הידע המרכזי של המערכת

> **מסמך זה הוא מקור האמת היחיד של הפרויקט.**
> מבוסס על שיחות האפיון והפיתוח המלאות.
> יש לקרוא אותו בתחילת כל סשן עבודה לפני כל פעולה.
> יש לעדכנו בכל החלטה מהותית חדשה.

---

## 1. מהות המיזם

**הבית הצהוב (Habayit Hatsahov)** — אינדקס עסקים והטבות של קהילת אוהדי מכבי תל אביב, מאוהדים לאוהדים.

- **ללא מטרות רווח בשלב זה**
- עסקים בבעלות אוהדים מפרסמים את עצמם
- אוהדים מאושרים נהנים ממגוון עסקים ומהנחות בלעדיות
- האתר **סגור לחלוטין לאנשים שאינם רשומים ומאושרים** — מלבד דפי ההרשמה

---

## 2. ארכיטקטורה טכנית (החלטות שהתקבלו)

| שכבה | טכנולוגיה | סטטוס |
|------|-----------|--------|
| Frontend | Pure HTML / CSS / JavaScript — PWA | ✅ פעיל |
| פונט | Heebo (Google Fonts) | ✅ פעיל |
| כיוון | RTL (`dir="rtl"`, `lang="he"`) | ✅ פעיל |
| Backend / DB | Firebase (Auth + Firestore + Hosting) | ⏳ לא חובר |
| Storage | Firebase Storage — תמונות עסקים, לוגואים, תפריטים | ⏳ לא חובר |
| מיילים | EmailJS — אוטומטי בעת אישור חבר (חינמי עד 200/חודש) | ⏳ לא חובר (נשמר כמשימה) |
| שרת פיתוח | server.ps1 — PowerShell local | ✅ פעיל |
| שמירת כרטיס | html2canvas (scale=3) | ✅ פעיל |

> **כל הנתונים כרגע hardcoded ב-JS. Firebase יחליף זאת.**

---

## 3. פלטת צבעים (Brand — חוקי ברזל)

| משתנה | ערך | שימוש |
|--------|------|--------|
| `--yellow` | `#FFD700` | צבע ראשי — כפתורים, הדגשות |
| `--yellow-dark` | `#E6C200` | hover, גרדיאנטים |
| `--blue` | `#003DA5` | hero, כותרות, רקע |
| `--blue-dark` | `#002a75` | topbar, footer, כרטיסי חבר |
| `--blue-mid` | `#1a56c4` | גרדיאנטים |
| `--orange` | `#d97706` | שגיאות, badge "ממתין" |

**⛔ חוק ברזל: אין אדום בשום מקום בפרויקט. שגיאות = כתום בלבד.**

---

## 4. סוגי משתמשים והרשאות

### 4.1 אורח (Guest)
- גישה ל-`index.html` — הרשמת אוהד
- גישה ל-`business.html` — הרשמת עסק (**פתוח לכולם, ללא צורך להיות חבר מאושר קודם**)
  - הטופס כולל בתוכו אימות אוהד מובנה

### 4.2 חבר מאושר (Approved Fan Member)
- גישה ל-`home.html` — אינדקס עסקים
- גישה ל-`business-page.html` — עמוד עסק בודד
- גישה ל-`profile.html` — פרופיל אישי הכולל תעודת חבר (**חשוף לחבר מאושר**)
- גישה ל-`card.html` — תעודת חבר standalone
- חשיפת קופונים ומימוש הטבות

### 4.3 מנהל (Admin)
- גישה ל-`admin.html` — פאנל ניהול
- אישור/דחיית בקשות הצטרפות
- **⚠️ אין כרגע מנגנון אימות — admin.html נגיש לכולם. יש לאבטח עם Firebase Auth.**

---

## 5. מבנה הדפים

```
index.html           ← דף נחיתה + הרשמת אוהד (אורחים)
business.html        ← הרשמת עסק לאינדקס (אורחים + חברים, כולל אימות אוהד)
home.html            ← אינדקס עסקים ראשי — חברים מאושרים
business-page.html   ← עמוד עסק מלא (?id=N) — לשיתוף לינקים
profile.html         ← פרופיל אישי + תעודת חבר + הגדרות — חברים מאושרים
card.html            ← תעודת חבר standalone
admin.html           ← פאנל ניהול
nav.html             ← ⚠️ DEV ONLY — למחיקה לפני העלאה
tasks.html           ← ⚠️ DEV ONLY — למחיקה לפני העלאה
```

### זרימת ניווט
```
[אורח]
index.html ──(מילוי טופס)──→ ממתין לאישור מנהל
business.html ──(מילוי טופס)──→ ממתין לאישור מנהל

[מנהל]
admin.html ──(אישור)──→ נשלח WhatsApp + מייל לאוהד

[חבר מאושר]
home.html
  ├── לחיצה על עסק → bottom sheet קופץ (חוויה ראשית)
  ├── "פתח דף מלא" → business-page.html?id=N (לשיתוף)
  └── לחיצה על פרופיל → profile.html → card.html
```

---

## 6. אפיון כל דף

### 6.1 דף נחיתה והרשמת אוהד (`index.html`)

**Hero:**
- לוגו מכבי (SVG רשמי, צבוע צהוב) עם אנימציית float
- כותרת "הבית הצהוב"
- Tagline + כפתור CTA + Stats strip

**4 Feature cards:** מחזקים עסקים / ספקים מהימנים / הנחות / משפחה

**טופס הרשמה — שדות:**
- שם פרטי, שם משפחה, טלפון, אימייל
- **אימות אוהד — 2 נתיבים:**
  - מנוי פעיל → בחירת שער (1/2/7/8/11/13) + מספר מנוי
  - לא מנוי → "האם היית מנוי?" + תמונה מהאצטדיון + קישור פייסבוק
- שאלה "בעל עסק?" → **לא חובה, מופיעה בתחתית הטופס** → מי שענה "כן" מקבל למייל לינק להרשמת עסק לאחר אישור חברות
- **הוספת בני משפחה (עד 2):** שם, קרבה, טלפון, אימייל — אופציונלי
- תנאי שימוש + כפתור שליחה

**כללי בני משפחה (מאפיין שהוחלט):**
- עד 2 בני משפחה תחת חשבון אחד
- אישור המשתמש הראשי = אישור אוטומטי לבני המשפחה
- כל בן משפחה מקבל תעודת חבר נפרדת

### 6.2 פאנל ניהול (`admin.html`)

**Stats row:** סה"כ נרשמים / ממתינים / מאושרים

**רשימת בקשות (expand/collapse per כרטיס):**
- Avatar (אותיות ראשונות), שם, אימייל, status badge
- **זמן המתנה (מאפיין שהוחלט):**
  - 🟢 עד 6 שעות — "fresh"
  - 🟠 עד 24 שעות — "medium"
  - 🔴 מעל יום — "long"
- **תאריך אישור (מאפיין שהוחלט):** כשמאשרים — מוצג "✓ אושר ב-[תאריך ושעה]"
- Detail grid: טלפון, אימייל, שער, מנוי, עסק?
- כפתורי אישור / דחייה
- כפתור WhatsApp (פותח עם הודעה מוכנה — ידני)
- כפתור מייל (פותח לקוח מייל — ידני)

**EmailJS (לא מחובר עדיין — משימה לעתיד):**
- בעת אישור — מייל נשלח אוטומטי ללא פעולה ידנית

### 6.3 תעודת חבר דיגיטלית (`card.html`, מוטמע גם ב-`profile.html`)

**מתי נוצרת:** רק לאחר אישור מנהל (לא בהרשמה)

**מה מוצג על התעודה (מאפיין שהוחלט):**
- לוגו "הבית הצהוב" + לוגו מכבי
- שם מלא
- מספר חבר (ממוספר אוטומטי, `#XXXXX`)
- תאריך הצטרפות
- שדה "שער" — רק אם הוא מנוי פעיל
- "חבר מאושר ✓"
- **אין:** תמונה, תוקף

**סגנון:** כרטיס אשראי שוכב, aspect-ratio 1.586, רקע כחול, פרטים בצהוב

**איך האוהד מקבל ומשתמש:**
- נשלח כלינק אישי בווטסאפ ובמייל לאחר אישור
- נגיש באזור האישי (`profile.html`)
- כפתור "שמור לגלריה" (html2canvas scale=3)
- מציג בחנות פיזית מהמסך / מהגלריה

**בני משפחה:** תעודה נפרדת לכל אחד, כולל "בן/בת משפחה של [שם]"

**עיצוב רקע בלומפילד:** נשמר כמשימה לעתיד (קובץ `stadium.jpg` + gradient overlay)

### 6.4 אינדקס עסקים (`home.html`) — סגנון Wolt

**Top nav (sticky):**
- לוגו + שורת חיפוש + כפתור פרופיל (מחליף את תעודת החבר)
- אין bottom navigation bar

**Category grid:**
- 2 שורות, עיגולים, **ללא גלילה אופקית**
- 8 קטגוריות: 🍕 אוכל / 🔧 בעלי מקצוע / 👕 ביגוד / 💊 בריאות / 📊 פיננסים / 💻 טכנולוגיה / ⚖️ משפטים / ⚽ ספורט
- לחיצה על קטגוריה → grid view (2 עמודות)

**סקציות (scroll אופקי per סקציה):**
1. ✨ **חדש אצלנו** — לפי `isNew` + `newUntil` (משך הזמן מוגדר ע"י מנהל לכל עסק)
2. 💛 **הטבות שוות** — עסקים עם `discount`
3. 🔥 **הכי פופולריים** — Top 5 לפי `clicks`
4. 🛒 **קניה אונליין** — עסקים עם `online: true`
5. 📍 **קרוב אליך** — בקשת GPS, Euclidean distance sorting

**כרטיס עסק (Business Card):**
- לוגו העסק (תמונה / אותיות ראשונות על גרדיאנט קטגוריה)
- שם העסק, קטגוריה
- **ההנחה מוצגת ישירות על הכרטיסייה** (לא צריך ללחוץ פנימה לגלות)
- עיר / "אונליין"
- Badges: ✨ חדש / 🌐 אונליין / `#N` דירוג פופולריות

**Analytics (מאפיין שהוחלט):**
- כל לחיצה על עסק = click נרשם
- **הקליק עצמו הוא האנליטיקס** — אין צורך בכפתור "חשוף" נפרד
- נשמר ב-localStorage (`_hb_clicks_{id}`) עד Firebase

**חשיפת הטבה:**
- **אונליין** → קוד קופון מוצג ישירות (ללא לחיצת חשיפה — מוחלט)
- **פיזי** → הוראה להציג תעודת חבר

### 6.5 חוויית עסק — Bottom Sheet + דף מלא

**חוויה ראשית במובייל: Bottom Sheet (מוחלט)**
- עולה מלמטה, מכסה ~70% מסך
- לא יוצאים מהאינדקס
- רקע מטושטש מאחורה

**בדסקטופ:**
- אותו תוכן כ-modal מרכזי עם backdrop
- מוצג באמצע המסך (max-width 520px)

**`business-page.html` — נשמר לשיתוף לינקים בלבד**
- URL נפרד לכל עסק (`?id=N`)
- כל התוכן המלא + כל פיצ'רי ה-sheet

### 6.5.1 שדרוגי דף/sheet העסק — מתוכנן ומאושר

**שינוי עיצוב Hero (מוחלט):**
- רקע: גרדיאנט כחול (`#002a75` → `#003DA5`) **במקום** צבעי קטגוריה
- לוגו "הבית הצהוב" (SVG + טקסט) — **top-right של ה-hero**
- לוגו העסק (קטן) — **מצד ימין ליד שם העסק**, לא במרכז ה-hero

**תוכן מורחב (מוחלט לבנות):**

| פיצ'ר | קהל | פירוט |
|-------|-----|--------|
| תיאור "מה העסק מציע" | כולם | טקסט חופשי, אופציונלי |
| גלריית תמונות | כולם | מקום / מוצרים / אוכל / שירותים |
| סרטונים | כולם | וידאו של המקום / מוצרים |
| **תפריט** | `cat: 'food'` בלבד | תמונת PDF / פריטים מובנים |
| קישור לאתר בולט | `online: true` | כפתור "בקר באתר" |
| טלפון + פרטי קשר | כולם | טלפון, WhatsApp, אימייל |
| שעות פעילות | עסקים פיזיים | per יום + "פתוח/סגור עכשיו" |
| כתובת + ויז/מפות | עסקים פיזיים | כתובת מלאה + deep link |

**תוספות שאושרו לשקול:**
- תג "מאומת ע"י הבית הצהוב" — בונה אמון
- כפתור שיתוף (Web Share API)
- "עסקים דומים שאולי תאהב" — carousel תחתית
- כפתור "שמור לפייבוריטים" — רשימה ב-profile
- "X חברים צפו בהטבה" — סושיאל פרוף (כבר קיים חלקית)

### 6.6 הרשמת עסקים (`business.html`)

**פתוח לכולם — אורחים וחברים. כולל אימות אוהד מובנה.**

**6 סקציות:**
1. פרטי בעל העסק (שם, טלפון, אימייל)
2. פרטי העסק (שם, תיאור, עיר, טלפון עסק)
3. קטגוריה — 9 pill grid (כולל `sport` ו-`other` שלא קיימים עדיין ב-home)
4. הטבה לחברים — toggle + סוג מימוש + קוד קופון (אם אונליין)
5. תמונות — לוגו (1) + גלריה (עד 5) — preview מיידי
6. רשתות חברתיות — אתר, אינסטגרם, פייסבוק
+ **אימות אוהד** (זהה ל-index.html)

**הסבר קופון לבעל עסק (נכתב בקוד):**
> "את הקוד האוהדים יוכלו לממש את ההטבה באתר שלכם (רצוי קוד שהוא רק לאוהדי מכבי)"

### 6.7 פרופיל אישי (`profile.html`)

**חשוף לחבר מאושר בלבד.**

- Top bar + חזרה ל-home
- Hero: avatar (אותיות ראשונות), שם, תאריך הצטרפות, "חבר פעיל"
- **תעודת חבר מוטמעת** (aspect-ratio 1.586)
- כפתור "שמור תעודה לגלריה"
- פרטים אישיים: שם, טלפון, אימייל, סטטוס, שער
- הגדרות: toggle מיקום, בני משפחה (UI בלבד), toggle התראות

---

## 7. מודל הנתונים

### אובייקט עסק (Business)

```js
{
  // קיים כרגע (hardcoded)
  id, name, cat, city, desc,
  discount, discountType,     // 'physical' | 'online'
  coupon,                     // רק אם online
  isNew, newUntil,            // משך ה"חדש" מוגדר ע"י מנהל
  popular, clicks, online,
  lat, lng,                   // לסינון מיקום
  phone, website, instagram,

  // מתוכנן (Firebase)
  logo,                       // URL — Firebase Storage
  photos,                     // URL[] — גלריה
  videos,                     // URL[] — סרטונים
  menu,                       // URL — רק food
  address,                    // כתובת מדויקת
  hours,                      // { sun, mon, ... } שעות פעילות
  facebook,
  isVerified,                 // מאומת ע"י הבית הצהוב
  savedBy,                    // uid[] — חברים ששמרו לפייבוריטים
  approvedAt,                 // תאריך אישור המנהל
  newUntilDate,               // מוגדר ע"י מנהל
}
```

### אובייקט חבר (Member)

```js
{
  // קיים (hardcoded mock: "ישראל ישראלי #00142 שער 8")
  firstName, lastName,
  memberId,           // #00142 — ממוספר אוטומטי
  joinedDate,
  gate,               // שם שער | null
  isSubscriber,

  // מתוכנן (Firebase)
  phone, email,
  status,             // 'pending' | 'approved' | 'rejected'
  approvedAt,
  isBusinessOwner,
  familyMembers: [{ name, relation, phone, email }],
  savedBusinesses,    // id[]
  locationAllowed,
}
```

### LocalStorage — שימושים נוכחיים

| מפתח | תוכן |
|------|-------|
| `locationAllowed` | `'true'` / `'false'` |
| `_hb_clicks_{id}` | ספירת צפיות per עסק |
| `_hbTasks` | מצב משימות DEV |

---

## 8. קטגוריות עסקים

| key | תווית | emoji | צבעי לוגו | קיים ב-home |
|-----|--------|-------|-----------|------------|
| `food` | אוכל ושתייה | 🍕 | `#FF6B35` → `#E85D04` | ✅ |
| `pro` | בעלי מקצוע | 🔧 | `#2563EB` → `#1D4ED8` | ✅ |
| `fashion` | ביגוד ואופנה | 👕 | `#7C3AED` → `#6D28D9` | ✅ |
| `health` | בריאות ויופי | 💊 | `#059669` → `#047857` | ✅ |
| `finance` | פיננסים | 📊 | `#0284C7` → `#0369A1` | ✅ |
| `tech` | טכנולוגיה | 💻 | `#0891B2` → `#0E7490` | ✅ |
| `legal` | משפטים | ⚖️ | `#64748B` → `#475569` | ✅ |
| `sport` | ספורט ופנאי | ⚽ | TBD | ❌ רק business.html |
| `other` | אחר | 📦 | TBD | ❌ רק business.html |

> **⚠️ אי-עקביות:** `sport` ו-`other` קיימים ב-business.html אך לא ב-home.html. לסנכרן בעת חיבור Firebase.

---

## 9. חוקים עסקיים

1. **רק אוהדי מכבי** — האינדקס סגור לציבור.
2. **אימות אוהד בשני נתיבים** (קיים ב-index.html וב-business.html):
   - מנוי פעיל → שער + מספר מנוי
   - לא מנוי → תמונה מאצטדיון + קישור פייסבוק
3. **בעל עסק יכול להירשם ישירות** ב-business.html ללא צורך להיות חבר קודם.
4. **עד 2 בני משפחה** — אישור ראשי = אישור אוטומטי לבני משפחה.
5. **תעודת חבר נוצרת רק לאחר אישור מנהל.**
6. **תעודה נפרדת לכל בן משפחה**, כולל ציון "בן/בת משפחה של [שם]".
7. **ספירת clicks = analytics** — הלחיצה עצמה מספיקה, אין "כפתור חשיפה" מיותר.
8. **קוד קופון מוצג ישירות** בלחיצה על עסק (לא דורש לחיצה נוספת לחשיפה).
9. **עסק "חדש"** = `isNew: true` AND `new Date() < newUntil` — משך מוגדר ע"י מנהל.
10. **"פופולריים"** = Top 5 ממוין לפי `clicks`.
11. **תפריט** — פיצ'ר ייחודי לקטגוריה `food` בלבד.

---

## 10. מה בנוי / מה לא

### ✅ בנוי

| דף | מה קיים |
|----|---------|
| `index.html` | Hero, 4 features, טופס הרשמה מלא, 2 נתיבי אימות, בני משפחה, success state |
| `admin.html` | לוח בקרה ראשי — תאריך, 2 כרטיסי ניווט (חברים/עסקים) עם נרשמו+ממתינים, ניווט ל-admin-members / admin-businesses |
| `admin-members.html` | ניהול חברים — stats, פילטר (הכל/ממתינים/מאושרים/נדחו), חיפוש, כרטיסים קומפקטיים עם מנוי/לא מנוי inline, modal אישור עם WA+מייל, ביטול אישור |
| `admin-businesses.html` | ניהול עסקים — stats, פילטר, חיפוש, כרטיסים קומפקטיים עם מנוי/לא מנוי inline, modal אישור עם WA+מייל |
| `business.html` | 6 סקציות, קטגוריות pill, toggle הטבה, upload תמונות עם preview, אימות אוהד |
| `card.html` | כרטיס אשראי, לוגו, שם, #חבר, תאריך, שער, שמירה לגלריה |
| `home.html` | Top nav, category grid 2 שורות, 5 סקציות scroll, business cards עם הנחה, bottom sheet, analytics clicks, location banner |
| `business-page.html` | עמוד מלא לשיתוף, hero, פרטים, הטבה, social, bottom CTA |
| `profile.html` | Avatar, hero, תעודה מוטמעת, פרטים, הגדרות |

### ⏳ לא בנוי

| פיצ'ר | סטטוס |
|-------|--------|
| Firebase Firestore | לא חובר |
| Firebase Auth | לא חובר — אין הגנה על דפים |
| Firebase Storage | לא חובר — uploads לא נשמרים |
| EmailJS — אוטומטי | **משימה שמורה** |
| שדרוגי דף עסק (סעיף 6.5.1) | **מתוכנן — הצעד הבא** |
| רקע בלומפילד בתעודה | **משימה שמורה** (`stadium.jpg`) |
| Apple/Google Wallet | לא בנוי |
| עסקים שמורים (פייבוריטים) | לא בנוי |
| ביקורות חברים | לא בנוי |
| sport/other ב-home.html | לא מסונכרן |
| הגנת admin | לא בנוי |

---

## 11. אלמנטים DEV ONLY — למחיקה לפני העלאה

| מה | היכן |
|----|------|
| `nav.html` | מחיקת קובץ |
| `tasks.html` | מחיקת קובץ |
| `#_devBar` (כפתור "← תפריט") | כל הדפים |
| `#_tasksPanel` + script | index.html, home.html, business.html |

---

## 12. בעיות ידועות

| בעיה | חומרה |
|------|--------|
| admin.html ללא הגנה — נגיש לכולם | גבוהה |
| כל הדפים נגישים ללא auth | גבוהה |
| uploads לא נשמרים (Firebase Storage חסר) | גבוהה |
| sport/other לא מסונכרנים בין business לhome | בינונית |
| clicks ב-localStorage — לא מסונכרן בין מכשירים | בינונית |
| מיקום: Euclidean, לא קילומטרים (Haversine) | נמוכה |

---

## 13. עסקי Mock (9 דוגמאות hardcoded)

| שם | קטגוריה | עיר | הטבה | קופון |
|----|----------|-----|------|-------|
| מאפיית הצהוב | food | תל אביב | 10% על כל קנייה | — |
| Studio D – עיצוב גרפי | tech | רמת גן | 20% על פרויקט ראשון | MACCABI20 |
| שיפוצים ובנייה – משה | pro | פתח תקווה | ייעוץ ראשוני חינם | — |
| פארם פלוס | health | תל אביב | 15% על תוספי תזונה | — |
| חנות הצהוב – מרצ'נדייז | fashion | אונליין | קנה 2 קבל 1 | BUY2GET1 |
| יוסי ביטוח | finance | חולון | פגישת ייעוץ חינם | — |
| עו"ד גיל לוי | legal | תל אביב | שעת ייעוץ ראשונה ב-50% | — |
| קפה הבית – מכבי | food | גבעתיים | קפה שני ב-1 ₪ | — |
| TechFix – תיקון מכשירים | tech | אונליין | 10% על כל תיקון | TECHFIX10 |

---

## 14. תוכנית עבודה — מה שנשאר

| עדיפות | משימה |
|--------|--------|
| 1 | **שדרוג business.html** — דף הרשמת עסקים |
| 2 | **שדרוג business-page.html / bottom sheet** — עיצוב hero חדש + תוכן מורחב (גלריה, שעות, תפריט, מפה) |
| 3 | **סבב עיצוב** — חידוד כל הדפים ביחד |
| 4 | **Firebase Firestore** — הרשמות, חברים, עסקים |
| 5 | **Firebase Auth** — login, session, הגנה על דפים |
| 6 | **EmailJS** — מייל אוטומטי בעת אישור |
| 7 | **Firebase Storage** — תמונות, גלריות, תפריטים |
| 8 | **PWA + Hosting + דומיין** |
| 9 | **רקע בלומפילד** בתעודת החבר (`stadium.jpg`) |
| 10 | **Apple/Google Wallet** |

---

## 15. מצב נוכחי

**שלב: UI מלא, ללא Backend**

- כל 7 הדפים בנויים — UX/UI מוכנים
- נתונים hardcoded (9 עסקים, חבר "ישראל ישראלי #00142")
- **דשבורד ניהול שודרג** — פוצל ל-3 קבצים: admin.html (hub), admin-members.html, admin-businesses.html
- **הצעד הבא:** שדרוג דף הרשמת עסקים (`business.html`)
